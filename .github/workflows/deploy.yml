name: Build and Deploy on AWS
on: 
  push: 
    branches:
      - main
jobs: 
  build:
    runs-on: self-hosted
    steps: 
      - name: Check out repository code
        uses: actions/checkout@v3
      - name: Make a new backend envfile
        uses: SpicyPizza/create-envfile@v1.3
        with: 
          env_key_PORT: ${{secrets.PORT}}
          env_key_DATABASE_URL: ${{secrets.DATABASE_URL}}
          env_key_POSTGRES_USERNAME: ${{secrets.POSTGRES_USERNAM}}
          env_key_POSTGRES_PASSWORD: ${{secrets.POSTGRES_PASSWORD}}
          env_key_POSTGRES_DB: ${{secrets.POSTGRES_DB}}
          directory: back-end
          file_name: .env
          fail_on_empty: true
      - name: Make a new frontend envfile
        uses: SpicyPizza/create-envfile@v1.3
        with: 
          env_key_PORT: ${{secrets.VITE_APP_BACKEND_URL}}
          directory: front-end
          file_name: .env
          fail_on_empty: true
      - name: Run docker compose down
        run: docker compose down
      - name: Delete react docker volume
        run: if docker volume ls --quiet --filter name=passwordgenerator_react-volume | then docker volume rm passwordgenerator_react-volume; fi
      - name: Build docker images
        run: docker compose build
      - name: Run docker compose up
        run: docker compose up
